<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blood">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- SMS PERMISSION -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SimpleBlog"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".guidlines"></activity>
        <activity android:name=".about" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".sms" />
        <activity android:name=".Find_and_confirm_popup" />
        <activity android:name=".Blood_group_popup" />
        <activity android:name=".Recomended" />
        <activity android:name=".Search_popup" />
        <activity android:name=".Email_popup" />
        <activity android:name=".List_Of_Users" />
        <activity
            android:name=".Request_page"
            android:noHistory="true" />
        <activity
            android:name=".Updateprofile"
            android:noHistory="false" />
        <activity
            android:name=".Profile"
            android:noHistory="true" />
        <activity
            android:name=".Next1"
            android:noHistory="false" />
        <activity
            android:name=".Success"
            android:noHistory="true" />
        <activity
            android:name=".Phonesignup"
            android:noHistory="true" />
        <activity
            android:name=".Next"
            android:noHistory="true" />
        <activity
            android:name=".VerifyPhone"
            android:noHistory="false" />
        <activity
            android:name=".SignUp"
            android:noHistory="true" />
        <activity android:name=".Login" />
        <activity
            android:name=".Splash"
            android:noHistory="true" />
        <activity
            android:name=".MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>